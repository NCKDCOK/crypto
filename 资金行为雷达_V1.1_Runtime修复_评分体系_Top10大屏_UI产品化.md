# 资金行为雷达 V1.1：Runtime 修复 + 评分体系 + Top10 数据大屏 + 用户化表达 + UI 产品化改造



本轮不是推倒重写，也不是继续无边界加功能。

本轮目标是：

> **先修复当前 Runtime 的真实问题，再把系统从“工程调试页”升级成“用户一眼就知道现在该看什么”的资金行为雷达。**

项目仍然不是自动交易系统。

系统核心仍然是：

`资金异动 → 疑似启动 → 假启动过滤 → 确认启动 → 趋势延续 → 衰竭 → 撤离`

本轮新增产品目标：

> **首页只展示置信度和机会最高的 Top10，并把复杂指标翻译成普通用户能看懂的语言。**

UI 风格采用：

> **科技感 + Apple 式克制和丝滑**

不是“赛博朋克堆特效”，也不是传统后台管理系统。

---

# 一、本轮必须遵守的原则

## 1. 不推倒重写

当前项目的以下分层继续保留：

- collectors
- features
- detectors
- health
- state_machine
- replay
- alerts
- api
- runtime

原则：

`integrate > repair > calibrate > productize`

不要为了界面重构底层分析逻辑。

---

## 2. 不实现自动交易

禁止：

- 下单
- API Key
- 用户账户
- 仓位管理
- 杠杆设置
- 开仓和平仓
- 订单同步
- 自动交易执行

系统只做：

> 行情扫描、资金行为分析、评分、解释、提醒、历史回放。

---

## 3. Evidence-first 保留

新增“机会分”和“子评分”以后，不能丢掉 Evidence-first。

任何分数必须满足：

`分数 → 可展开 → 原始 Evidence → 原始数据`

例如：

`资金输入 91`

点击后必须看到：

- OI 1m +1.8%
- OI 5m +4.7%
- Delta Z +3.2
- CVD slope +2.9σ
- Price Efficiency 0.78

禁止出现：

> “资金输入 91”

但系统无法解释 91 怎么来的。

---

# 二、先修当前版本的 P0 问题

在开始新 UI 和评分体系之前，先完成以下修复。

---

# P0.1 修复 Stage1 Candidate 100 问题

当前出现：

- Universe = 100
- Candidate = 100
- Deep = 40

这说明 Stage1 基本没有发挥筛选作用。

必须检查现有 LightScanner。

禁止使用：

`score > 0`

这种只要 24h 涨跌非 0 就成为候选的逻辑。

Stage1 的职责是：

> 从 Universe 中找出“刚刚突然不正常”的币。

预期：

`Universe 100`

↓

`Candidate 10~25`

↓

`Deep Scan 10~20`

候选数必须随着市场状态自然变化，而不是接近 Universe 全量。

---

# P0.2 Stage1 改为短时增量异动

不要直接用 24h 累计值本身做异常。

Binance `/ticker/24hr` 可以继续使用，但必须计算相邻采样之间的增量：

- ΔQuoteVolume
- ΔTradeCount
- ΔPrice
- ΔQuoteVolume / Δt
- ΔTradeCount / Δt
- ΔPrice / Δt

例如：

`t0 quoteVolume = 200M`

`t1 quoteVolume = 201.8M`

真正有意义的是：

`ΔQuoteVolume = +1.8M`

而不是：

`当前 24h 成交额 = 201.8M`

对以下数据维护 rolling baseline：

- 20s 成交额增量
- 20s 成交笔数增量
- 20s 价格变化
- 20s OI 变化（可用时）

计算：

- quote_volume_delta_z
- trade_count_delta_z
- price_delta_z
- oi_delta_z

Stage1 应输出：

`ANOMALY_CANDIDATE`

而不是直接 LONG / SHORT。

---

# P0.3 真正接入 1m / 5m / 15m / 1h Kline

当前配置声明：

- 1m
- 5m
- 15m
- 1h

但实际 Runtime 必须确认四个周期全部收到真实数据。

可接受两种方案：

## 方案 A

直接订阅：

- symbol@kline_1m
- symbol@kline_5m
- symbol@kline_15m
- symbol@kline_1h

## 方案 B

只订 1m，然后本地聚合高周期。

第一版优先简单可靠。

验收：

任意 Deep Scan symbol 详情必须能显示：

- 1m context LIVE
- 5m context LIVE
- 15m context LIVE
- 1h context LIVE

禁止 Feature 名义存在但长期 `None`。

---

# P0.4 Deep Candidate 加防抖

当前候选集合变化时，不要整批停止所有 collectors 再重新启动。

加入：

## Candidate Hysteresis

进入 Deep Set 后：

- 最低驻留 3~5 分钟
- 连续 N 次跌出阈值后才移除

新 symbol：

- 增量 subscribe

移除 symbol：

- 增量 unsubscribe

不要每 20 秒整组重连。

目标：

- 减少 WS 重建
- 减少 UNKNOWN
- 减少 OI/Funding 断档
- 提升 Freshness 稳定性

---

# P0.5 修复 UI 中“数据 false”

用户页面禁止直接显示内部布尔值。

例如：

`数据：false`

必须翻译成：

- `数据正常`
- `数据降级`
- `数据延迟`
- `数据异常`

Circuit Breaker 细节只在健康页显示。

用户首页顶部显示：

`● DATA LIVE`

或：

`● 数据正常`

---

# P0.6 清理旧 DashboardService

检查：

`src/api/dashboard.py`

如果旧 DashboardService 已经不再被 Runtime 使用：

- 删除
- 或标记 deprecated
- 或正式统一到新 runtime data model

禁止长期存在两套：

- latest_state
- latest_events
- dashboard cache

避免以后 AI 误用。

---

# 三、产品结构重新设计

最终页面拆成 6 个主要页面。

---

# 页面 1：首页 / 资金雷达大屏

这是默认首页。

目标：

> 用户打开后 5 秒知道“现在有没有值得看的机会”。

首页只展示：

> **Top10**

不要把 Universe 100/200 全部铺出来。

顶部总览：

- DATA LIVE / DEGRADED
- Universe 数量
- 当前候选
- 异常
- 疑似启动
- 确认启动
- 延续
- 衰竭
- 撤离

并增加一句“系统结论”。

例如：

`当前无确认启动机会，建议等待。`

或者：

`当前发现 2 个确认启动机会，优先关注 ONGUSDT。`

这句话必须是规则生成，不要由大模型自由发挥。

---

# 四、首页 Top10 卡片

每张卡必须回答：

1. 这是什么币？
2. 方向是什么？
3. 当前处于什么阶段？
4. 它为什么排在这里？
5. 风险是否正在变大？
6. 数据是否可信？

推荐卡片结构：

```text
#1 ONGUSDT                     LONG

0.09303                +27.61%

机会分                         86.4
置信度                         92%

状态：🚀 启动确认

资金输入      91
启动质量      94
趋势          88
即时续航      86
持续启动      90

追涨安全      74
顶部风险      21
拥挤风险      18
撤离风险      12

OI 1h        +6.58%
Funding      +0.005%

一句话：
新增仓位与主动买盘同步增强，
启动后资金仍在持续进入。

[查看为什么]
```

---

# 五、首页只展示 Top10，其余放“全市场”

Universe 和用户列表必须分开。

首页：

- Top10

全市场页面：

- 100 / 200 / 全市场

这样用户不会一打开就面对 100 个币。

---

# 六、全市场页面

必须支持搜索。

搜索框：

`搜索 BTC / ETH / DOGE / ONG`

必须支持：

- symbol 模糊搜索
- symbol 精确搜索

筛选：

- 全部
- 确认启动
- 趋势延续
- 疑似启动
- 异动观察
- 衰竭
- 撤离
- 数据异常

周期：

- 1m
- 5m
- 15m
- 1h

排序：

- 机会分
- 置信度
- 启动
- 资金输入
- 续航
- OI
- 24h 涨跌
- 最近更新时间

---

# 七、全市场表格字段

建议：

| 排名 | 币种 | 机会 | 置信度 | 状态 | 方向 | 资金 | 启动 | 趋势 | 续航 | 追涨 | 顶部 | OI | Funding |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|

不要在全市场页面放太多原始技术字段。

原始数据进入详情页。

---

# 八、币种详情页

点击任意 symbol 后进入详情。

## 8.1 顶部结论

例如：

```text
ONGUSDT

🚀 启动确认
方向：做多

机会分：86.4
置信度：92%
数据状态：正常
```

下面给一句用户能看懂的话：

> 当前属于“新增多头资金持续进入”的启动阶段，主动买盘、OI 与成交量同步增强，暂未发现明显撤离迹象。

禁止用户首页出现：

- CVD divergence
- Delta Z
- acceptance
- oi_accel

这种只有开发者才懂的词作为第一层结论。

这些放二级展开。

---

## 8.2 资金行为模块

用户层：

- 主动买盘：强
- 新增仓位：明显
- 资金持续性：良好
- 拥挤程度：低
- 撤离迹象：暂无

展开后：

- taker buy
- taker sell
- delta
- CVD
- OI
- Funding

---

## 8.3 量价模块

用户层：

- 成交量：明显放大
- 价格推动效率：健康
- 回踩承接：良好
- 突破有效性：已确认

展开后：

- Volume Z
- Trade Count Z
- Price Efficiency
- Retrace
- Acceptance

---

## 8.4 假启动检查

用户看到：

```text
假启动检查

✅ 无快速回吐
✅ OI 未收缩
✅ 主动买盘未反转
✅ 突破后仍能站稳
✅ 未出现明显放量滞涨
✅ 数据正常
```

开发者模式展开原始 Veto。

---

## 8.5 状态时间轴

例如：

```text
14:31  发现异动
14:32  疑似启动
14:34  启动确认
14:41  趋势延续
```

不要只显示：

`ANOMALY → SUSPECTED_START`

用户层必须中文化。

---

# 九、信号中心

保存历史状态变化。

展示：

```text
14:42

ONGUSDT
🚀 启动确认
方向：做多

机会分：86
置信度：92%

资金输入持续增强，
假启动过滤全部通过。
```

其他状态：

- 异动观察
- 等待确认
- 启动确认
- 趋势延续
- 衰竭警告
- 资金撤离

未来 Telegram / 飞书 / Discord 直接复用这一层数据。

---

# 十、回放验证页面

用于验证评分是否有效。

至少展示：

- 过去 7 天确认启动次数
- 假启动拦截次数
- 1m / 5m / 15m / 1h 后方向表现
- 最大有利波动
- 最大不利波动
- 机会分分桶表现

例如：

```text
机会分 >= 80

样本：83
15m 后正向：68%
1h 后正向：73%
平均最大正向：+5.8%
平均最大反向：-2.1%
```

后续评分校准必须基于 Replay 数据。

---

# 十一、数据健康页面

开发者/系统维护使用。

展示：

| Symbol | AggTrade | Kline | OI | Funding | Queue Lag | Confidence |
|---|---|---|---|---|---|---|

状态翻译：

- LIVE → 正常
- DEGRADED → 部分降级
- STALE → 数据过期
- ERROR → 数据异常

首页不要暴露内部字段。

---

# 十二、评分体系重新引入

本轮允许加入分数。

但必须采用：

> **可解释评分**

---

# 十三、子评分设计

至少：

## 1. 资金输入

回答：

> 新增方向资金是否真的进入？

基于：

- OI expansion
- taker delta
- CVD slope
- CVD acceleration
- price response

## 2. 启动质量

回答：

> 这次异动是不是像真正启动？

基于：

- volume anomaly
- trade count anomaly
- price acceleration
- OI confirmation
- flow confirmation
- breakout acceptance

## 3. 趋势

回答：

> 当前方向是否稳定？

基于：

- 1m
- 5m
- 15m
- 1h

不要直接用 RSI / MACD 作为主逻辑。

## 4. 即时续航

回答：

> 最近几十秒到几分钟，资金还在不在？

基于：

- delta persistence
- CVD persistence
- OI persistence
- price efficiency
- retrace quality

## 5. 持续启动

回答：

> 启动是不是只有一波，还是有持续性？

## 6. 异动强度

回答：

> 当前变化相对历史是否异常？

基于：

- quote volume delta Z
- trade count delta Z
- price delta Z
- OI delta Z

## 7. 追涨安全

回答：

> 现在是不是已经太晚？

基于：

- distance from initiation
- retrace
- efficiency
- crowding
- local extension

## 8. 顶部风险

回答：

> 是否正在出现“买盘很大但价格推不动”等衰竭迹象？

基于：

- CVD divergence
- high volume no progress
- OI decay
- efficiency collapse
- failed breakout

## 9. 拥挤风险

基于：

- Funding
- Premium
- Long/Short context

## 10. 撤离风险

基于：

- OI decay
- delta reversal
- CVD reversal
- efficiency collapse
- failed acceptance

---

# 十四、机会分

机会总分不是简单平均。

先实现一个透明、配置化的版本。

结构：

```text
基础机会
=
资金输入
+ 启动质量
+ 趋势
+ 即时续航
+ 持续启动
+ 异动强度

风险扣分
=
顶部风险
+ 拥挤风险
+ 撤离风险
+ 追涨风险
```

最终：

`OpportunityScore 0~100`

权重全部放配置文件。

禁止 magic number。

每个 score 输出 breakdown。

---

# 十五、置信度必须独立于机会分

机会分回答：

> 这个机会本身好不好？

置信度回答：

> 我们对这个判断有多大把握？

Confidence 应受：

- Data Health
- Evidence completeness
- Multi-window consistency
- Missing source
- stale source

影响。

例如：

```text
ONG
机会分 88
置信度 92%
```

和：

```text
XXX
机会分 91
置信度 47%
```

后者不能排在前者前面。

---

# 十六、Top10 排名

推荐独立定义：

`RankingScore`

第一版可透明实现：

`RankingScore = OpportunityScore × ConfidenceFactor`

ConfidenceFactor：

`0~1`

以后 Replay 再校准。

首页 Top10 按 RankingScore 排。

用户只看到：

- 机会分
- 置信度

不需要显示 RankingScore。

---

# 十七、用户语言翻译层

新增独立模块：

`PresentationTranslator`

或等价层。

职责：

> 把技术字段翻译成普通用户能看懂的中文。

---

# 十八、内部字段与用户文案映射

例如：

| 内部术语 | 用户页面 |
|---|---|
| ANOMALY | 发现异动 |
| SUSPECTED_START | 等待确认 |
| START_CONFIRMED | 启动确认 |
| CONTINUATION | 趋势延续 |
| EXHAUSTION | 动能衰竭 |
| WITHDRAWAL | 资金撤离 |
| CONFIDENT | 数据可信 |
| DEGRADED | 部分数据降级 |
| UNKNOWN | 数据不足 |
| OI_EXPANSION | 新增仓位增加 |
| DELTA_REVERSAL | 主动资金方向反转 |
| RAPID_RETRACE | 快速回吐 |
| NO_ACCEPTANCE | 突破未站稳 |
| LOW_EFFICIENCY_ABSORPTION | 放量但价格推不动 |
| CVD_DIVERGENCE | 资金推动开始背离 |

---

# 十九、禁止把内部术语直接塞给普通用户

首页禁止默认显示：

- zscore
- CVD slope
- delta ratio
- acceptance
- oi_acceleration
- confidence enum
- circuit open

这些进入：

`高级数据`

或：

`为什么？`

展开面板。

---

# 二十、用户第一层必须是结论

例如技术层：

`volume_z=4.7`

用户层：

`成交量明显放大`

技术层：

`oi_change_5m=+4.7%`

用户层：

`新增仓位明显增加`

技术层：

`price_efficiency=0.21`

用户层：

`买盘很多，但价格推动效率偏低`

---

# 二十一、系统必须告诉用户“还缺什么”

对于 SUSPECTED_START：

不要只显示：

`等待确认`

而要显示：

```text
还缺 2 项确认：

• OI 持续扩张
• 第二波主动买盘确认
```

这样用户知道为什么还不能升级。

---

# 二十二、系统必须允许“没有机会”

首页必须支持：

```text
当前暂无确认启动机会。

有 3 个异动币正在观察，
尚未通过资金确认与假启动过滤。
```

禁止为了让页面“有内容”强行推荐币。

---

# 二十三、视觉设计方向

采用：

> **科技风数据终端 + Apple 式克制、空间感和丝滑交互**

关键词：

- 深色
- 清晰
- 高对比
- 少量霓虹
- 大留白
- 柔和圆角
- 层级明显
- 动效克制
- 信息密度高但不拥挤

---

# 二十四、颜色原则

建议：

- 背景：深灰蓝黑，而不是纯黑
- 主色：冷青 / 蓝青
- LONG：绿色
- SHORT：红色
- Warning：橙色
- Neutral：灰色
- Score：青色或蓝白渐变

禁止：

- 全页面大量荧光描边
- 每个卡片都发光
- 赛博朋克式 RGB 污染
- 过度动画

---

# 二十五、Apple 式“丝滑”体现

重点不是模仿 Apple 官网。

而是：

- hover 轻微浮起
- 卡片展开自然过渡
- 数字变化平滑
- Tab 切换无页面闪烁
- Skeleton loading
- 页面局部更新
- 不整页刷新
- 筛选状态保留
- 搜索即时响应
- 移动端自然布局

动画：

`150~250ms`

优先 CSS transition。

---

# 二十六、首页布局

建议：

## Header

左：

`CRYPTO MARKET RADAR`

副标题：

`资金行为 · 量价分析 · 启动识别`

右：

- DATA LIVE
- Universe
- 更新时间
- 搜索

## Market Summary

四到八个轻量指标：

- 确认启动
- 延续
- 疑似启动
- 异动
- 衰竭
- 撤离

## Top10 Radar

第一名可以做 Hero Card。

其余 9 个做标准卡片。

## Selected Symbol Preview

点击 Top10 卡片时：

页面下半部分直接显示：

- 为什么
- 资金
- 风险
- 状态时间线

不一定立刻跳转详情页。

---

# 二十七、Hero Card

Top1 视觉权重最高。

例如：

```text
#1 ONGUSDT

机会分
86.4

置信度
92%

🚀 启动确认 · LONG

资金输入   91
启动质量   94
续航       86

新增仓位明显增加，
主动买盘持续占优，
当前尚未出现明显撤离迹象。
```

---

# 二十八、响应式设计

Desktop：

- 4~5 列 cards

Tablet：

- 2~3 列

Mobile：

- 1 列
- Top10 横滑也可以
- 详情采用底部抽屉

必须保证移动端可用。

---

# 二十九、搜索

搜索必须全局存在。

支持：

- `/` 快捷键聚焦
- symbol 搜索
- Enter 进入详情

例如：

输入：

`eth`

立即出现：

`ETHUSDT`

---

# 三十、空状态

所有页面必须设计空状态。

例如首页无机会：

```text
暂无确认启动机会

系统正在监控 100 个交易对。
当前有 3 个异动候选，
但尚未通过启动确认。

继续等待资金信号。
```

不要出现空白表格。

---

# 三十一、Loading 状态

禁止一打开页面：

`未知 未知 未知 未知`

数据尚未准备时显示：

- 正在加载成交数据
- 正在建立 OI 基线
- 正在同步 Kline
- 数据预热中

例如：

`系统预热 42%`

只有真正缺失/异常才叫“未知”。

---

# 三十二、评分预热

新 symbol 刚进入 Deep Scan 时：

不要立即给 0 分或 UNKNOWN 分。

状态：

`数据预热中`

当最小样本达到要求：

- Volume baseline
- CVD baseline
- OI baseline
- multi-timeframe context

才开始评分。

---

# 三十三、详情页支持“普通模式 / 专业模式”

默认：

`普通模式`

显示中文结论。

可切换：

`专业模式`

显示：

- Z-score
- CVD
- Delta
- raw OI
- efficiency
- thresholds

这样兼顾新手和开发者。

---

# 三十四、API 输出也要分层

API 不要只输出 raw fields。

建议：

```json
{
  "symbol": "ONGUSDT",
  "state": "START_CONFIRMED",
  "state_label": "启动确认",
  "direction": "LONG",
  "direction_label": "做多",
  "opportunity_score": 86.4,
  "confidence": 0.92,
  "summary": "新增仓位与主动买盘同步增强，启动后资金仍在持续进入。",
  "subscores": {},
  "evidence": [],
  "vetoes": []
}
```

Presentation 层可以复用。

---

# 三十五、评分测试

增加测试：

- score range 0~100
- no evidence → no fake high score
- stale data → confidence drop
- missing OI → confidence drop
- strong startup fixture → high startup score
- fake pump fixture → false start veto
- withdrawal fixture → high withdrawal risk

---

# 三十六、UI 测试

至少验证：

- Top10 排序正确
- 搜索正确
- 筛选正确
- 没机会时空状态正确
- UNKNOWN 不进入 Top10
- stale symbol 不进入高置信 Top10
- Evidence 展开正常
- 普通模式翻译正确
- 专业模式 raw data 正确

---

# 三十七、本轮禁止事项

不要提前增加：

- SMC
- OB
- FVG
- Fibonacci
- Machine Learning
- Q-learning
- 神经网络
- 自动交易
- 多交易所

先把：

> **量价资金行为 + 真启动识别 + 评分 + 用户体验**

做好。

---

# 三十八、本轮执行顺序

严格按照：

1. Runtime Audit
2. 修 Candidate 100
3. Stage1 改短时增量
4. Kline 多周期接入
5. Candidate Hysteresis
6. Runtime 稳定性
7. Score Engine
8. Confidence Engine
9. Translation Layer
10. Top10 Ranking
11. 首页数据大屏
12. 全市场搜索
13. Symbol Detail
14. 信号中心
15. 数据健康
16. Replay 评分验证
17. UI polish
18. Tests
19. Documentation

---

# 三十九、完成定义 DoD

只有满足全部条件才能声明本轮完成：

## Runtime

- Candidate 不再等于 Universe
- Stage1 使用短时增量
- 1m/5m/15m/1h 都有真实数据
- Deep candidate 不再频繁整组重连
- Data Health 正常
- UNKNOWN 明显减少
- stale fail closed

## Score

- OpportunityScore 可解释
- Confidence 独立存在
- 至少 8 个子评分
- 每个分数可展开 Evidence
- 风险分独立展示
- Score 全部配置化

## UX

- 首页只显示 Top10
- 全市场单独页面
- 支持搜索
- 支持筛选
- 用户文案中文化
- 内部术语不直接暴露
- 普通模式 / 专业模式
- 无机会状态正确
- 加载状态正确

## UI

- 科技感
- 克制
- 深色
- Apple 式顺滑
- 响应式
- 不使用廉价赛博朋克风
- 不使用整页频繁刷新

## Test

- pytest 全部通过
- live smoke test 通过
- UI 核心交互通过
- Replay deterministic

---

# 四十、最终交付报告

完成后输出：

## Runtime Fixes

本轮修复了哪些问题。

## Scoring Model

列出所有分数及计算证据。

## UX Translation

列出内部术语到用户文案映射。

## UI Architecture

页面结构和组件。

## Real Data Status

- AggTrade
- OI
- Funding
- 1m
- 5m
- 15m
- 1h

必须明确 LIVE / NOT LIVE。

## Test Results

pytest 结果。

## Screenshots

至少：

- 首页 Top10
- 全市场
- Symbol Detail
- 数据健康

## Known Limitations

诚实列出。

---

# 最终产品体验目标

用户打开首页以后，不应该再想：

> “这么多币，我到底看哪个？”

而应该马上得到：

```text
当前 Top1：ONGUSDT

机会分：86
置信度：92%

状态：启动确认
方向：做多

原因：
新增仓位明显增加，
主动买盘持续增强，
价格推动效率健康，
假启动检查全部通过。

当前尚未发现明显撤离迹象。
```

如果当前没有机会：

```text
当前暂无确认启动机会。

有 3 个异动币正在观察，
但均未通过启动确认。
```

系统必须敢于告诉用户：

> **现在没有值得关注的机会。**

这也是有效结果。

开始执行。
