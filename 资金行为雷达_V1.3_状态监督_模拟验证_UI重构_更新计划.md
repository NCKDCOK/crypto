# 资金行为雷达 V1.3 更新计划
## 状态分区监督 × 推荐快照 × 模拟验证 × UI 信息架构重构

仓库：

`https://github.com/NCKDCOK/crypto`

---

# 0. 本轮定位

V1.3 不再以“继续增加指标”为主目标。

当前系统已经具备实时扫描、Feature、State、Score、Top10、Drawer、Trade Plan 等基础能力，但仍缺少最重要的一层：

> **机会出现以后，系统如何持续监管、如何验证自己当时的判断、如何记录后续结果。**

V1.3 的核心闭环：

```text
全市场扫描
  ↓
发现异动
  ↓
进入对应监督池
  ↓
持续监督状态变化
  ↓
形成正式推荐
  ↓
冻结推荐快照
  ↓
自动加入模拟观察队列
  ↓
等待参考关注区
  ↓
二次验证
  ↓
模拟入场
  ↓
持续监督资金行为
  ↓
TP / 失效 / 撤离 / 方向反转
  ↓
模拟退出
  ↓
记录 MFE / MAE / 结果
  ↓
进入统计与回放
  ↓
反向校准评分与规则
```

系统依旧不是自动交易系统。

禁止：

- 自动真实下单
- 私有交易 API Key
- 自动账户管理
- 自动仓位管理
- 自动设置杠杆
- 自动开平仓

模拟系统只允许 Paper / Shadow Trading。

---

# 1. 本轮优先级

本轮优先级严格如下：

## P0：先修当前会污染判断的问题

1. 首页和排名刷新节奏稳定化
2. 修复 OI 单位、OI 1h、OI 百分比展示
3. 修复 Taker B/S 标签和真实计算
4. Top10 不允许 COOLDOWN / 无信号币硬凑
5. Trade Plan 只允许合法状态生成
6. Data Health 改为覆盖率，不再“一币异常=全局异常”
7. 删除普通模式 / 专业模式切换
8. 长时间停机后的 Startup Recovery

## P1：新增状态监督体系

9. State Pool Manager
10. Supervisor Engine
11. 不同状态使用不同监督频率和规则
12. 状态迁移与生命周期管理

## P2：新增推荐快照与模拟系统

13. Immutable Recommendation Snapshot
14. Simulation Queue
15. Entry Revalidation
16. Paper Position
17. Simulation Exit Engine
18. MFE / MAE / TP / Stop / Withdrawal 记录
19. 结果统计

## P3：UI 重构

20. 首页精简
21. 全市场保留
22. 新增监督台
23. 信号中心并入监督台 / 模拟记录
24. 回放验证并入模拟验证
25. 独立详情页取消，统一右侧 Drawer
26. 数据健康保留，但降低导航优先级

---

# 2. UI 信息架构重新设计

当前页面不应该继续无限增加。

建议最终主导航只保留 5 个核心入口：

```text
[首页]
[全市场]
[监督台]
[模拟验证]
[数据健康]
```

如果保留设置：

```text
[设置]
```

放在右上角图标，不占主导航。

---

# 3. 建议删除 / 合并的多余界面

## 3.1 删除独立“币种详情页”

原因：

- 已经确认用户更喜欢右侧 Drawer
- 跳页会打断首页扫描节奏
- 同一份详情数据维护两个页面没有意义

处理：

- 所有 symbol 点击行为统一打开右侧 Drawer
- 原 `/symbol/:symbol` 路由可以保留兼容，但自动打开对应 Drawer 或重定向首页

---

## 3.2 “信号中心”不再作为单独核心页面

当前信号中心只是状态事件卡片流，信息密度不高，而且会产生大量重复卡片。

建议把它拆成两部分：

### A. 实时状态迁移

并入：

`监督台 → 状态日志`

### B. 正式推荐 / 模拟结果

并入：

`模拟验证 → 推荐记录`

因此主导航可以删除独立“信号中心”。

---

## 3.3 “回放验证”并入“模拟验证”

新的模拟验证页包含：

```text
[运行中]
[等待入场]
[已结束]
[统计]
[历史回放]
```

因此没有必要再单独保留“回放验证”。

---

# 4. 最终推荐页面结构

## 首页

回答：

> **现在最值得看什么？**

## 全市场

回答：

> **我想主动搜索 / 筛选某个币。**

## 监督台

回答：

> **这些已经进入生命周期的币，现在分别处于什么阶段，系统正在重点盯什么？**

## 模拟验证

回答：

> **系统过去推荐的东西到底有没有用？现在有哪些模拟单正在等待、运行、结束？**

## 数据健康

回答：

> **数据源有没有问题？哪些 symbol 正在预热或降级？**

---

# 5. 新增 State Pool Manager

不要再把所有 Deep Symbols 当成相同对象处理。

新增：

`StatePoolManager`

把 symbol 按当前生命周期分配到不同监督池。

---

# 6. 状态池定义

## 6.1 NORMAL_POOL / 普通扫描池

包含：

- SLEEPING
- COOLDOWN
- 长期无异常

职责：

> 低成本扫描，寻找新的异常。

监督频率：

`15 ~ 30 秒`

关注：

- 短时成交额增量
- Trade Count 增量
- Price acceleration
- 简单 OI 变化

---

## 6.2 ANOMALY_POOL / 异动观察池

包含：

- ANOMALY
- Dormant Revival 初期
- 异常放量

职责：

> 判断这次异动是噪声、Pump，还是进入真正资金生命周期。

监督频率：

`5 ~ 10 秒`

重点：

- Delta
- CVD
- OI
- Price Efficiency
- Rapid Retrace
- Spot / Perp
- Funding
- Pump Risk

---

## 6.3 WATCH_POOL / 重点观察池

包含：

- SUSPECTED_START
- ACCUMULATION
- RETEST_PENDING
- Breakout Candidate

职责：

> 等待正式确认。

监督频率：

`1 ~ 5 秒`

重点：

- 5m 收盘
- Breakout Hold
- Acceptance
- Retest
- Spot Confirmation
- OI persistence
- CVD persistence
- Second Impulse

---

## 6.4 CONFIRMED_POOL / 确认机会池

包含：

- START_CONFIRMED

职责：

> 正式推荐、冻结快照、生成 Trade Plan、加入模拟队列。

必须：

- Snapshot
- Trade Plan
- Simulation Queue
- 正式 Push

---

## 6.5 CONTINUATION_POOL / 趋势跟踪池

包含：

- CONTINUATION

职责：

> 不再判断“有没有启动”，而是判断“资金还在不在”。

重点：

- OI persistence
- CVD persistence
- Taker persistence
- Retest health
- Second / Third impulse
- Distribution Risk
- Withdrawal Risk

---

## 6.6 RISK_POOL / 风险池

包含：

- EXHAUSTION
- DISTRIBUTION
- 高顶部风险
- 高撤离风险

职责：

> 判断是否应该结束当前 Setup。

重点：

- CVD divergence
- OI decay
- Delta reversal
- High volume no progress
- Price Efficiency collapse
- Failed breakout
- Spot sell pressure

---

## 6.7 EXIT_POOL / 撤离池

包含：

- WITHDRAWAL
- DIRECTION_FLIP
- INVALIDATED

职责：

- 关闭当前 Setup
- 结束模拟仓位
- 写入结果
- 进入归档

---

## 6.8 ARCHIVE_POOL / 历史归档

包含：

- REJECTED
- EXPIRED
- CLOSED
- INVALIDATED

只用于：

- Replay
- Statistics
- Calibration

---

# 7. Supervisor Engine

新增：

`SupervisorEngine`

职责不是发现新币，而是：

> **已经进入某个生命周期的币，接下来应该重点监督什么。**

每个 symbol 应维护：

```text
symbol
current_pool
current_state
setup_type
entered_pool_at
entered_state_at
last_transition_at
supervision_level
next_check_at
```

---

# 8. 不同状态不能共用同一套监督条件

例如：

## ANOMALY

问：

> 这次异常是真的吗？

## SUSPECTED_START

问：

> 还缺哪些确认条件？

## START_CONFIRMED

问：

> Trade Plan 是否成立？是否进入模拟观察？

## CONTINUATION

问：

> 资金是否继续？

## EXHAUSTION

问：

> 是正常休息，还是派发 / 撤离？

## WITHDRAWAL

问：

> Setup 是否正式结束？

监督逻辑必须 state-aware。

---

# 9. 状态升级 / 降级

每个池必须定义：

- Enter conditions
- Stay conditions
- Upgrade conditions
- Downgrade conditions
- Expire conditions
- Vetoes

例如：

```text
ANOMALY
↓
SUSPECTED_START
```

必须满足一定 Evidence。

而不是：

> 分数瞬间超过一个阈值就直接跳。

---

# 10. 状态滞回

状态必须加入 hysteresis。

例如：

```text
START_CONFIRMED
```

不能因为下一秒机会分从 82 变 78 就回到 SUSPECTED_START。

需要：

- 连续 N 次失去核心条件
- 或发生明确 Veto
- 或 Breakout invalidated

才降级。

---

# 11. 首页不再直接展示“实时 Engine 当前值”

新增：

`DecisionSnapshotService`

首页展示：

> 稳定决策快照

而不是后台每一秒的 Feature 最新值。

---

# 12. 首页刷新规则

建议：

| 内容 | 更新频率 |
|---|---|
| 当前价格 | 3~5 秒 |
| 24h 涨跌 | 30 秒 |
| 资金摘要 | 10 秒 |
| 子评分 | 15~30 秒 |
| Opportunity | 30 秒 |
| Signal Confirmation | 30 秒 |
| Top10 排名 | 60 秒 |
| 状态 | 仅状态迁移 |
| Trade Plan | 仅 Setup 生成 / 更新 |

后台仍然可以 1~2 秒实时计算。

---

# 13. Top10 不再强制凑满 10 个

这是重要修改。

首页模块名称仍可叫：

`Top Opportunities`

但是：

> **最多 10 个，不是必须 10 个。**

只有满足以下门槛才进入：

```text
state in [START_CONFIRMED, CONTINUATION]
Opportunity >= configured_min
Signal Confirmation >= configured_min
Data Confidence >= configured_min
Trade Plan valid
not stale
not pump_risk_high
```

如果只有 2 个，就显示 2 个。

如果 0 个：

```text
当前暂无确认机会。
系统正在重点观察 5 个候选。
```

绝不让 COOLDOWN 成为首页 #1。

---

# 14. 首页新增“重点观察”区域

Top Opportunities 下方增加一个小区域：

`正在观察`

最多 5 个。

可进入：

- ANOMALY
- SUSPECTED_START
- ACCUMULATION
- RETEST_PENDING

显示：

```text
STXUSDT
疑似启动
还缺：OI持续 + 5m收盘确认
```

不要给正式 Trade Plan。

---

# 15. 首页顶部字段重构

现有顶部字段过多且部分偏 Debug。

建议顶部只保留：

```text
市场背景
数据健康
Universe
重点观察数
确认机会数
风险中数量
```

示例：

```text
市场：山寨偏弱 · BTC稳定 · 流动性一般
数据：92% 正常
Universe：100
观察：5
确认：2
风险：1
```

不要首页展示：

- circuit open
- 内部 Boolean
- collector count
- queue size

这些只放数据健康页。

---

# 16. 首页卡片字段重构

建议首页每个正式机会卡片只显示：

```text
Symbol
当前价
24h
状态
Setup
主周期
方向

机会分
信号确认
数据可信

资金输入
启动质量
持续启动
即时续航
吸筹迹象
追涨安全
撤离风险

一句话结论
当前阶段
当前计划摘要
```

不适用字段显示 `—`。

不建议首页直接显示大量：

- 原始 CVD
- Delta Z
- OI raw
- Funding raw
- Volume Z

这些放 Drawer。

---

# 17. 右侧 Drawer 重新设计

Drawer 是完整分析中心。

推荐顺序：

## A. 当前结论

```text
Symbol
当前价
24h
状态
Setup
方向
主周期
快照时间
```

## B. 核心评分

```text
机会分
信号确认
数据可信
```

## C. 当前计划

仅正式状态显示：

```text
参考关注区
结构失效位
TP1
TP2
TP3
R:R
当前是否建议追
```

## D. 生命周期

```text
发现异动
疑似启动
确认启动
趋势延续
衰竭
撤离
```

当前节点突出。

## E. 评分明细

```text
资金输入
吸筹
启动
持续启动
即时续航
趋势
回踩健康
现货确认
沉睡复活
逼空

追涨安全
顶部风险
派发风险
拥挤风险
撤离风险
Pump风险
```

## F. 资金摘要

```text
RVOL
Taker B/S
Delta
CVD
OI 5m
OI 15m
OI 1h
Funding
Premium
Spot CVD
Perp CVD
```

## G. Breakout Lifecycle

```text
突破时间
突破位
突破保持
回撤深度
首次回踩
二次确认
距局部高点
```

## H. Evidence / Veto

核心证据 / 辅助证据 / 否决项。

## I. 模拟状态

如果已进入 Simulation：

```text
等待入场
已模拟入场
当前浮盈亏
MFE
MAE
退出原因
```

---

# 18. Trade Plan 状态限制

严格限制：

## 不生成正式计划

- SLEEPING
- COOLDOWN
- ANOMALY

## 只能生成候选预案

- SUSPECTED_START
- ACCUMULATION
- RETEST_PENDING

UI 必须写：

`候选预案，尚未确认`

## 正式 Trade Plan

- START_CONFIRMED
- CONTINUATION

---

# 19. Trade Plan 必须冻结版本

每次正式 Setup 创建：

```text
trade_plan_id
version
created_at
setup_snapshot_id
entry_zone
invalidation
tp1
tp2
tp3
rr
status
```

禁止每秒随价格重算并覆盖旧计划。

后续变化只能：

```text
V1 → EXPIRED
V2 → NEW PLAN
```

---

# 20. Immutable Recommendation Snapshot

新增：

`RecommendationSnapshot`

正式进入首页推荐时自动冻结一份不可修改的快照。

至少记录：

```text
snapshot_id
symbol
timestamp
market_regime
state
setup_type
direction
primary_timeframe

current_price
opportunity_score
signal_confirmation
data_confidence

all_subscores
all_evidence
all_vetoes

breakout_state
structure_state
spot_perp_state

trade_plan
```

快照生成后：

> 绝对不能被后来的实时值覆盖。

---

# 21. 为什么必须冻结快照

如果系统今天 03:00 推荐：

```text
ONG
机会 86
确认 91
```

03:20 变成：

```text
机会 55
撤离风险 72
```

历史记录必须保留：

> 03:00 当时系统到底看到了什么。

否则 Replay 会产生未来数据污染。

---

# 22. 自动加入 Simulation Queue

任何正式首页推荐：

```text
START_CONFIRMED
```

并且满足 Top Opportunity 门槛后：

自动：

```text
RecommendationSnapshot
↓
SimulationQueue
```

状态：

`WATCHING`

---

# 23. Simulation 状态机

建议：

```text
WATCHING
↓
ENTRY_ZONE_REACHED
↓
REVALIDATING
↓
ARMED
↓
SIMULATED_ENTRY
↓
OPEN
↓
CLOSED
```

旁路状态：

```text
EXPIRED
CANCELLED
INVALIDATED
MISSED
```

---

# 24. WATCHING

正式推荐已经出现，但价格还没有进入合理观察区。

记录：

- 推荐时间
- 推荐价格
- Entry Zone
- 当前价格
- 距离 Entry Zone
- 最大上涨 / 下跌

---

# 25. ENTRY_ZONE_REACHED

当实时价格进入：

```text
reference_entry_low ~ reference_entry_high
```

不能直接模拟成交。

必须进入：

`REVALIDATING`

---

# 26. Entry Revalidation

重新检查原 Snapshot 的方向是否仍然成立。

至少检查：

```text
Data Health
State 仍合法
Setup 未失效
Breakout 未失效
Withdrawal 未触发
Direction 未翻转
OI 未严重反向
CVD 未严重反转
Spot/Perp 未明显恶化
Market Regime 未严重反转
Pump Risk 未突然升高
```

---

# 27. Revalidation 结果

## 通过

```text
REVALIDATING
→ ARMED
→ SIMULATED_ENTRY
```

## 不通过

```text
REVALIDATING
→ CANCELLED
```

记录取消原因。

例如：

```text
进入观察区，但多头资金结构已经失效。
```

---

# 28. Simulated Entry

模拟成交必须记录：

```text
simulation_id
snapshot_id
symbol
direction
entry_time
entry_price
entry_reason
entry_confirmation
```

第一版可使用：

- Entry Zone 内第一笔符合 Revalidation 的价格

后续再考虑：

- 模拟滑点
- Maker / Taker 模拟

---

# 29. 模拟仓位监督

一旦模拟入场，监督目标发生变化。

不再重点问：

> 还能不能入场？

而是问：

> 原 Setup 是否继续成立？

持续记录：

```text
current_pnl
MFE
MAE
current_state
fund_flow_change
continuation_change
withdrawal_risk_change
distribution_risk_change
```

---

# 30. MFE / MAE

必须记录：

## MFE

Maximum Favorable Excursion

> 模拟入场以后最大有利波动。

## MAE

Maximum Adverse Excursion

> 模拟入场以后最大不利波动。

它们是以后校准 Entry / Stop / TP 的核心数据。

---

# 31. 模拟退出原因

至少支持：

```text
TP1_HIT
TP2_HIT
TP3_HIT
INVALIDATION_HIT
SIGNAL_WITHDRAWAL
DISTRIBUTION_EXIT
DIRECTION_FLIP
TIME_EXPIRED
MANUAL_CLOSE
```

---

# 32. 同时记录“系统退出”和“原计划结果”

非常重要。

例如系统因为资金撤离提前退出：

```text
SIGNAL_WITHDRAWAL
```

也必须继续后台观察：

> 如果不提前退出，原 TP / Stop 最终会怎样？

得到两套结果：

## A. Dynamic Exit

资金撤离时退出。

## B. Static Plan

原 TP / Stop 固定执行。

以后比较：

> “跟资金撤”是否真的优于固定止盈止损。

---

# 33. Simulation Queue UI

新的“模拟验证”页面顶部：

```text
[运行中]
[等待入场]
[已结束]
[统计]
[历史回放]
```

---

# 34. 等待入场列表

字段：

```text
Symbol
Setup
方向
推荐时间
推荐价
参考关注区
当前价
距离关注区
机会分
确认度
当前状态
```

状态示例：

```text
等待回踩
进入关注区
二次验证中
已取消
已过期
```

---

# 35. 运行中模拟仓位

字段：

```text
Symbol
方向
Entry
Current
PnL
MFE
MAE
TP1
Invalidation
当前状态
资金变化
撤离风险
```

---

# 36. 已结束

字段：

```text
Symbol
Setup
方向
Entry
Exit
Result
Exit Reason
MFE
MAE
Duration
Opportunity
Confirmation
```

---

# 37. 统计页面

至少展示：

```text
推荐次数
进入观察区次数
通过 Revalidation 次数
模拟入场次数
TP1 次数
TP2 次数
失效次数
撤离退出次数
平均 MFE
平均 MAE
```

---

# 38. 分桶统计

按：

- Opportunity Score
- Signal Confirmation
- Setup Type
- Market Regime
- Direction
- Timeframe

统计。

例如：

```text
Opportunity 80~90
样本 47
TP1率 68%
平均 MFE +5.7%
平均 MAE -1.8%
```

---

# 39. Setup 统计

例如：

```text
回踩复燃
样本 31
模拟成交 24
TP1 18
失效 5
平均 MFE +6.2%
```

和：

```text
疑似吸筹 → 启动
样本 42
2h 内进入 START_CONFIRMED：27
转化率：64%
```

---

# 40. 监督台 UI

建议使用分区式布局，而不是大量重复卡片。

可以类似 Kanban：

```text
异动观察 | 等待确认 | 确认机会 | 趋势跟踪 | 风险 | 撤离
```

每列只展示核心字段。

---

# 41. 监督台卡片字段

```text
Symbol
状态
Setup
方向
进入本状态多久
机会分
确认度
当前监督重点
下一条件
```

例如：

```text
STXUSDT
等待确认 · LONG
已观察 4m12s

机会 64
确认 71%

正在等待：
5m 收盘站稳 + OI继续扩张
```

---

# 42. 信号历史不要重复卡片

当前同一 symbol 可能产生很多重复事件。

V1.3 改成：

> 一个 Setup 对应一条 Timeline。

例如：

```text
ONG-20260826-001

03:01 发现异动
03:03 疑似启动
03:05 5m突破
03:09 回踩
03:11 二次确认
03:12 正式推荐
03:24 趋势延续
03:46 动能衰竭
03:51 资金撤离
```

而不是创建 8 张互不关联的卡片。

---

# 43. 修复 OI 数据模型

当前必须明确区分：

```text
oi_change_abs_1m
oi_change_pct_1m

oi_change_abs_5m
oi_change_pct_5m

oi_change_abs_15m
oi_change_pct_15m

oi_change_abs_1h
oi_change_pct_1h
```

用户默认展示百分比。

例如：

```text
OI 5m +2.3%
OI 1h +6.1%
```

禁止：

> absolute change 后直接加 `%`

---

# 44. OI 历史窗口

必须足够支持真实 1h。

当前内存如果只保留十几分钟，需要扩展。

建议至少：

```text
OI snapshots >= 2h
```

或者进入本地 TimeSeries Store。

---

# 45. Taker B/S 修复

明确区分：

## Delta Ratio

```text
(Buy - Sell) / (Buy + Sell)
```

范围：

`-1 ~ +1`

用户名称：

`主动资金偏向`

## Taker B/S

```text
Taker Buy Volume / Taker Sell Volume
```

用户名称：

`Taker B/S`

例如：

`1.66`

禁止二者混用。

---

# 46. Data Health 改覆盖率

首页：

```text
数据健康 92%
```

而不是：

`数据异常`

只因为某一个币 OI 延迟。

建议：

```text
>= 90% 正常
70~90% 部分降级
< 70% 异常
核心数据源整体断线 = 严重异常
```

---

# 47. Startup Recovery

程序重启：

```text
BOOTSTRAP
RECOVERY
WARMUP
LIVE
```

只有 LIVE：

- 允许正式 Top Opportunity
- 允许 Push
- 允许自动创建 Simulation Snapshot

---

# 48. 重启数据策略

保留：

- 历史 Kline
- 历史 OI
- Funding
- Snapshot
- Simulation
- Replay

重新构建：

- 当前 State
- 当前 Opportunity
- 短周期 Delta
- 短周期 CVD
- 即时续航
- 当前 Top10

---

# 49. Historical Kline Store

如果尚未完成，V1.3 作为 P0 基础设施补齐。

逻辑：

```text
启动
↓
读取本地历史
↓
检查 gap
↓
只补 gap
↓
WS 增量继续
```

不要每次重新全量拉历史。

---

# 50. 历史状态不能直接跨长停机继承

例如停机 > 1h：

- 旧 START_CONFIRMED 失效
- 旧 CONTINUATION 失效
- 旧实时 Score 失效
- 旧 Trade Plan 检查是否 EXPIRED

然后根据最新历史重建。

---

# 51. Drawer 增加 Simulation 区块

当 symbol 已进入模拟系统：

显示：

```text
模拟状态：等待回踩
快照时间：03:12:00
快照价格：0.0930
参考关注：0.0900~0.0910
当前价：0.0918
距离关注区：0.9%
```

进入模拟仓位后：

```text
模拟 Entry
Current
PnL
MFE
MAE
Exit Trigger
```

---

# 52. 首页不显示过多 Simulation 细节

首页只加一个小状态：

```text
等待回踩
模拟跟踪中
资金衰减
```

完整信息在 Drawer / 模拟验证页。

---

# 53. 推荐卡片加入“监督阶段”

建议新增字段：

`监督阶段`

可能值：

```text
等待回踩
等待二次确认
确认启动
趋势跟踪
资金衰减
撤离观察
```

它比单纯的数字变化更有使用价值。

---

# 54. 分数趋势而不是秒级数值跳动

首页显示：

```text
资金输入 91 ↑
续航 86 →
追涨安全 61 ↓
```

箭头来自一段稳定窗口。

例如：

- 过去 30s / 1m 的平滑趋势

不是比较上一秒。

---

# 55. Opportunity / Confirmation 快照化

推荐以后：

首页主值保留当时 Snapshot：

```text
推荐机会分 86
```

实时变化可以在 Drawer 里显示：

```text
当前机会分 81 ↓
```

这样可以看到：

> 推荐时 vs 现在

而不是原来的数字被覆盖。

---

# 56. Drawer 推荐增加“双值”

例如：

```text
推荐时：
机会 86
确认 91

当前：
机会 78 ↓
确认 73 ↓
```

这对判断信号衰减非常有价值。

---

# 57. Simulation 不使用未来数据

严格禁止：

- 推荐后用未来 K 线重新修改 Snapshot
- 回看后重新优化 Entry 并覆盖历史
- 用结果数据反向改当时 Score

所有测试必须 chronological。

---

# 58. Paper Trading 与 Replay 使用同一核心规则

Live Simulation 和 Historical Replay 必须共用：

- Entry Revalidation
- Position Monitoring
- Exit Engine
- TP / Stop logic

避免：

> Live 一套，Replay 另一套。

---

# 59. 数据持久化建议

至少增加：

```text
recommendation_snapshots
simulation_queue
simulation_positions
simulation_events
simulation_results
state_transitions
```

可先使用 SQLite / DuckDB。

---

# 60. 推荐 Snapshot 表

至少字段：

```text
id
symbol
created_at
state
setup_type
direction
market_regime
price
opportunity
signal_confirmation
data_confidence
trade_plan_json
evidence_json
veto_json
features_json
```

---

# 61. Simulation Result 表

至少：

```text
simulation_id
snapshot_id
entry_time
entry_price
exit_time
exit_price
exit_reason
pnl_pct
mfe_pct
mae_pct
tp1_hit
tp2_hit
tp3_hit
invalidation_hit
dynamic_exit_price
static_plan_result
```

---

# 62. 配置化

所有阈值放配置。

例如：

```yaml
ranking:
  min_opportunity: 70
  min_signal_confirmation: 75
  min_data_confidence: 85
  max_items: 10

supervision:
  normal_interval_sec: 20
  anomaly_interval_sec: 7
  watch_interval_sec: 2

simulation:
  recommendation_expire_minutes: 60
  require_revalidation: true
```

不要 magic number。

---

# 63. API 设计建议

新增：

```text
GET /api/home
GET /api/market
GET /api/supervision
GET /api/supervision/{symbol}
GET /api/simulations
GET /api/simulations/{id}
GET /api/statistics
```

避免前端自己组合多个底层接口做决策。

---

# 64. 首页 API 返回稳定 Snapshot

不要返回每一秒变动的所有底层 Score。

首页返回：

```json
{
  "market_regime": {},
  "health": {},
  "confirmed_opportunities": [],
  "watch_candidates": [],
  "risk_candidates": []
}
```

---

# 65. 监督台 API

按 Pool 返回：

```json
{
  "anomaly": [],
  "watch": [],
  "confirmed": [],
  "continuation": [],
  "risk": [],
  "exit": []
}
```

---

# 66. 测试要求

必须新增以下测试。

---

## 66.1 State Pool

- ANOMALY 正确进入异常池
- SUSPECTED_START 进入重点观察
- START_CONFIRMED 进入确认池
- CONTINUATION 进入趋势跟踪
- EXHAUSTION 进入风险池
- WITHDRAWAL 进入退出池

---

## 66.2 State Hysteresis

- 单次评分下降不降级
- 连续失去证据才降级
- 明确 Veto 可以立即失效

---

## 66.3 Recommendation Snapshot

- Snapshot immutable
- Snapshot 不被后续实时数据覆盖
- 相同 Setup 版本化

---

## 66.4 Simulation Queue

- 正式推荐自动加入
- COOLDOWN 不加入
- SUSPECTED_START 不加入正式模拟

---

## 66.5 Entry Revalidation

- 到达 Entry Zone + 原方向有效 → 入场
- 到达 Entry Zone + Withdrawal → 取消
- 到达 Entry Zone + Direction Flip → 取消
- 数据 stale → 不入场

---

## 66.6 Position Monitoring

- MFE 正确
- MAE 正确
- TP1 / TP2 / TP3 正确
- Stop 正确
- Withdrawal Exit 正确

---

## 66.7 Static vs Dynamic Exit

必须能同时记录：

- 系统动态退出结果
- 原固定计划结果

---

## 66.8 UI

- 首页不秒级重排
- Top10 不强制凑满
- COOLDOWN 不进入 Top Opportunity
- Drawer 内容稳定
- Simulation 状态正确
- 监督台 Pool 正确

---

# 67. Live Smoke Test

新增长期 smoke test：

至少运行：

`30 ~ 60 分钟`

检查：

- 状态池迁移
- Snapshot 创建
- Simulation Queue
- 首页排名稳定性
- OI 单位
- Taker B/S
- Data Health coverage
- 重连
- lag
- 内存

---

# 68. 本轮完成定义 DoD

V1.3 只有满足以下条件才能完成。

## 决策层

- 首页不再每秒重排
- Top Opportunity 不强制 10 个
- COOLDOWN / 无信号币不会成为 Top1
- Trade Plan 只在合法状态出现

## 数据层

- OI 百分比正确
- OI 1h 为真实 1h
- Taker B/S 正确
- Data Health 使用覆盖率
- Startup Recovery 可用

## 监督层

- 状态分池完整
- 每个池有独立监督规则
- 状态迁移有滞回
- 生命周期可追踪

## 模拟层

- 正式推荐自动生成 Snapshot
- Snapshot immutable
- 自动加入 Simulation Queue
- Entry Zone 到达后重新验证
- 模拟 Entry
- 模拟 Exit
- MFE / MAE
- TP / Stop
- Withdrawal Exit
- 结果持久化

## UI

- 首页
- 全市场
- 监督台
- 模拟验证
- 数据健康

其余冗余页面已合并或删除。

## 统计

至少能看到：

- 推荐次数
- 模拟入场次数
- TP1率
- 失效率
- 平均 MFE
- 平均 MAE
- Setup 分组统计

---

# 69. 最终首页目标

首页不要再像 Debug Dashboard。

最终应该类似：

```text
CRYPTO MARKET RADAR

市场：山寨偏弱 · BTC稳定 · 流动性一般
数据：92% 正常

确认机会 2
重点观察 5
风险中 1

━━━━━━━━━━━━━━━━━━━━

#1 ONGUSDT

当前价 0.09303       +27.6%

🚀 启动确认 · 回踩复燃 · LONG
主周期 5m

机会分      86
信号确认    91%
数据可信    96%

资金输入    91 ↑
启动质量    94 →
持续启动    90 ↑
即时续航    88 →
吸筹迹象    76 →
追涨安全    61 ↓
撤离风险    12

监督阶段：等待首次回踩

结论：
新增多头资金仍在进入，
现货与合约方向一致，
当前尚未发现明显撤离。

当前计划：
等待 0.090x ~ 0.091x 回踩重新确认。

模拟：等待入场
```

---

# 70. 最终监督台目标

```text
异动观察
  STX  异动 3m
  PUMP 放量但高Pump风险

等待确认
  HYPE  缺5m收盘
  POL   缺OI持续

确认机会
  ONG   等待回踩

趋势跟踪
  ETH   资金继续

风险
  WIF   CVD背离

撤离
  DOGE  OI+Delta同步衰减
```

用户应该一眼知道：

> 每个币现在系统正在重点看什么。

---

# 71. 最终模拟验证目标

```text
等待入场 3
运行中   2
今日结束 5

今日正式推荐：8
进入观察区：5
模拟入场：4
TP1：2
失效：1
撤离退出：1
```

点击任意 Simulation 可以看到完整：

```text
推荐快照
↓
价格如何发展
↓
为什么入场
↓
为什么退出
↓
如果固定 TP/Stop 会怎样
```

---

# 72. V1.3 最重要的产品原则

不是：

> 指标越多越专业。

而是：

> **系统对一个机会从发现到结束负责到底。**

每一个正式机会都应该拥有：

```text
发现记录
状态生命周期
推荐快照
Trade Plan
模拟观察
二次验证
模拟入场
持仓监督
模拟退出
最终结果
历史统计
```

最终形成真正闭环：

> **发现 → 监督 → 推荐 → 验证 → 跟踪 → 退出 → 统计 → 校准。**

这才是 V1.3 的完成目标。
