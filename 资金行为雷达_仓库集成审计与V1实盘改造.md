# 资金行为雷达：仓库集成审计与 V1.0 实盘数据改造任务

你现在接手一个已经完成基础骨架、但运行时集成尚不完整的 Python 项目。

项目不是自动交易机器人。

项目定位是：

> **资金行为驱动的实时行情分析雷达。**

核心目标不是预测价格，也不是堆 RSI / MACD，而是通过：

`Price × Volume × Aggressive Trade Flow × Open Interest × Funding/Premium`

判断市场中是否出现：

`资金异动 → 疑似启动 → 假启动过滤 → 确认启动 → 延续 → 衰竭 → 撤离`

最终输出可解释的市场雷达、证据链和提醒。

---

# 一、最高优先级原则

在修改任何代码前，完整阅读：

- `AI_RULES.md`
- `ARCHITECTURE.md`
- `SYSTEM_DESIGN.md`
- `AI_CODING_AGENT_MANUAL.md`
- `docs/DATA_MODEL.md`
- `docs/DATA_HEALTH.md`
- `docs/ANALYSIS_MODEL.md`
- `docs/STATE_MACHINE.md`
- `docs/TESTING.md`
- `specs/`

这些文件是项目 contract。

不要因为实现方便而随意违反已有设计。

如果现有设计与实际代码存在冲突：

1. 明确记录冲突。
2. 判断正确行为。
3. 优先保持系统设计语义。
4. 必要时更新文档。
5. 不允许静默改变 contract。

---

# 二、绝对禁止事项

本次改造禁止：

- 自动下单
- Binance 私有 API Key
- 账户余额
- 持仓同步
- 杠杆管理
- 开仓/平仓
- 订单管理
- 随意增加 RSI / MACD 等传统指标
- AI 自行发明“机会分”
- AI 自行拍脑袋决定指标权重
- 使用陈旧数据继续生成高置信度信号
- 为了 UI 好看制造假的评分
- mock 数据混入 production pipeline
- 把“资金行为推断”描述成已知庄家账户行为

系统只允许分析公开行情。

---

# 三、第一阶段：先做 Runtime Integration Audit

不要马上重构。

先完整审计整个 `src/`。

创建：`docs/RUNTIME_INTEGRATION_AUDIT.md`

对每一个重要模块列出：

| 模块 | 文件 | 已实现 | 已实例化 | Runtime已接入 | 收到真实数据 | 被谁消费 | 测试覆盖 | 问题 |
|---|---|---|---|---|---|---|---|---|

至少审计：

- AggTradeCollector
- KlineCollector
- OIPoller
- FundingCollector
- SymbolRegistry
- FeatureEngine
- RollingWindow
- FreshnessWatchdog
- QueueLagMonitor
- ConfidenceTracker
- RateLimiter
- AnomalyDetector
- StartupDetector
- FalseStartFilter
- ContinuationDetector
- WithdrawalDetector
- StateMachine
- Repository
- Replay
- AlertManager
- DashboardService

重点查找：

- orphan module
- 写了但从未 import
- import 了但从未实例化
- 实例化了但从未 start
- start 了但数据没有进入 FeatureEngine
- Feature 有定义但实际一直为 None
- hardcoded demo
- mock
- 强制状态
- 强制 confidence
- 空 Evidence 覆盖真实 Evidence
- magic number
- 时间单位不一致
- event_time / receive_time 混用
- stale 数据继续计算
- duplicate trade
- reconnect 后重复订阅
- websocket connected 但数据停止的问题

Audit 完成后再开始修改。不要只输出报告；报告完成后继续执行以下改造。

---

# 四、P0：修复当前 Runtime 的真实性

## P0.1 删除所有演示性 Confidence Override

当前系统不得出现类似 `confidence = CONFIDENT` 的硬编码。

Confidence 必须由 Data Health 实际计算，最少综合：

- aggTrade freshness
- kline freshness
- OI freshness
- funding freshness
- queue lag
- websocket health
- reconnect 状态
- source availability

输出：`CONFIDENT` / `DEGRADED` / `UNKNOWN`。

核心原则：`CONNECTED != HEALTHY`

如果 `now - last_event_time > freshness_budget`，对应数据源必须进入 `STALE`。

关键证据源 stale 时，`AnalysisState` 不允许升级到 `START_CONFIRMED`。Fail Closed。

---

# 五、P0.2 修复 Dashboard Evidence 被覆盖

目前必须明确区分两个概念：

### CurrentState

用于表示：

- 当前 symbol 状态
- 当前方向
- 当前 confidence
- 当前 features
- 当前 health

### TransitionEvent

用于表示：

- 从什么状态变为什么状态
- 当时使用了什么 Evidence
- 触发了什么 Veto
- 为什么发生状态迁移
- 发生时间

禁止为了刷新 Dashboard 每几秒构造 `evidence=[]`、`vetoes=[]` 的新 `AnalysisEvent` 覆盖真正的 transition event。

设计成：

- `latest_state[symbol]`
- `last_transition[symbol]`

分别存储。

Dashboard 首页读 CurrentState；详情页与信号历史读取 TransitionEvent。

Evidence 必须一直保留，直到下一次真实 transition。

---

# 六、P0.3 把已经存在的数据采集器全部接入 Runtime

目前不能只有 aggTrade。

必须真正接入：

## AggTrade

负责：

- 实时成交
- aggressor side
- taker buy/sell
- delta
- CVD
- trade count
- trade size
- large trade

## OI

负责：

- OI current
- OI Δ30s
- OI Δ1m
- OI Δ5m
- OI velocity
- OI acceleration

OI 的“新增仓位判断”优先使用 **contract/base quantity**。

不要直接用 `OI USD = OI × Price` 判断新增仓位。

USD OI 可以展示，但不得把单纯价格上涨造成的美元 OI 增长误判为新增仓位。

## Funding

负责：

- funding rate
- funding trend
- crowding context

## Kline

至少：

- 1m
- 5m
- 15m
- 1h

Kline 用于：

- 多时间尺度背景
- close-bar confirmation
- volume baseline
- breakout / acceptance
- retrace
- trend context

所有数据最终必须进入统一 FeatureSnapshot。

---

# 七、不要使用单一 30 秒窗口

当前系统需要多时间尺度滚动窗口。

Trade Flow 最低需要：

- 5s
- 15s
- 30s
- 1m
- 5m

Kline Context：

- 1m
- 5m
- 15m
- 1h

不要复制同一批数据多份造成巨大内存消耗。

应该设计 `WindowManager` 或等价结构。

FeatureEngine 从统一 WindowManager 获取不同时间尺度聚合结果。

---

# 八、P0.4 方向语义必须严格正确

重点检查 Binance `aggTrade` 的 maker flag。

需要通过 fixture 明确验证：

- buyer 是否 maker
- seller 是否 taker
- aggressor side
- taker buy
- taker sell

必须有测试：

### Case A

主动买：`delta > 0`

### Case B

主动卖：`delta < 0`

CVD：`CVD_t = CVD_(t-1) + signed_delta`

重连后必须支持 trade ID dedup，同一成交不得重复累计。

---

# 九、P1：从“三个币监控器”变成市场雷达

删除：`SYMBOLS = ["BTCUSDT", "ETHUSDT", "SOLUSDT"]` 这种生产运行硬编码。

使用已有 `SymbolRegistry`。

Universe 来自 Binance USDT-M 永续合约公开 market metadata。

必须：

- 只保留 ACTIVE / TRADING
- 只保留 USDT-M perpetual
- 排除已下架
- 支持 blacklist
- 支持 whitelist
- 支持最低成交额过滤
- 支持 configurable max symbols

第一版默认不要直接暴力扫描所有垃圾币。

建议 `TOP_N = 100` 或配置化。

排序依据优先：24h quote volume / liquidity。

后续支持 200 / 全市场。

---

# 十、实现两阶段 Radar，而不是所有币全开重型分析

## Stage 1 — Light Scanner

低成本扫描 Universe。

目标：找出哪些币“突然不正常”。

使用：

- price change
- relative volume
- volume acceleration
- trade count acceleration
- OI change
- volatility expansion

输出：`ANOMALY_CANDIDATE`

例如：100 个币 → 15 个候选。

## Stage 2 — Deep Scanner

只有候选进入深度资金行为分析。

使用：

- aggTrade flow
- taker delta
- CVD
- OI
- price efficiency
- retrace
- funding
- multi-window consistency
- later: OFI / depth

最终：

`ANOMALY → SUSPECTED_START → False Start Filter → START_CONFIRMED`

这样降低：

- websocket 压力
- CPU
- RAM
- REST 权重
- 418 / 429 风险

---

# 十一、核心 Feature Engine

V1 核心 Feature 至少包括：

## Price

- price_return_5s
- price_return_15s
- price_return_30s
- price_return_1m
- price_return_5m
- price_acceleration
- high_break
- low_break
- retrace_ratio
- acceptance

## Volume

- volume_5s
- volume_15s
- volume_30s
- volume_1m
- relative_volume
- volume_zscore
- volume_acceleration
- trade_count
- trade_count_zscore
- average_trade_size
- large_trade_ratio

## Flow

- taker_buy_volume
- taker_sell_volume
- signed_delta
- delta_ratio
- CVD
- CVD_slope
- CVD_acceleration

## OI

- oi_contracts
- oi_change_30s
- oi_change_1m
- oi_change_5m
- oi_velocity
- oi_acceleration

## Context

- funding
- premium（数据源可靠时加入）
- 1m context
- 5m context
- 15m context
- 1h context

## Quality

- source_age
- stale flag
- queue lag
- confidence state

禁止新增 Feature 以后却不展示其来源。

每一个 Feature 必须能追溯：`raw data → aggregation → feature`

---

# 十二、加入 Price Efficiency

这是核心 Feature，用于回答：

> 同样规模的主动资金，到底推动了多少价格？

至少实现可解释版本：

`price_efficiency = abs(price_return) / normalized_aggressive_flow`

注意：

- 防止除零
- 做 robust normalization
- 不同 symbol 不允许直接使用绝对数值比较
- 应基于自身 rolling baseline / z-score

资金越来越大但价格越来越推不动：`efficiency ↓`

可能代表：

- absorption
- distribution
- exhaustion

不要直接把它定义成卖出，它只是 Evidence。

---

# 十三、重新定义 Detector：Evidence First

Detector 不直接输出“机会分”。

输出 `Evidence[]`。

Evidence 至少包含：

- `code`
- `name`
- `direction`
- `raw_value`
- `normalized_value`
- `threshold`
- `window`
- `source`
- `asof`
- `explanation`

例如：`OI_EXPANSION_LONG`，而不是 `+15 分`。

---

# 十四、Anomaly Detector

职责只有：检测异常。

例如：

- volume anomaly
- trade-count anomaly
- price acceleration
- OI burst
- flow burst

输出：`ANOMALY`

不能直接输出 LONG / SHORT。

---

# 十五、Startup Detector

目标：判断是否存在新增方向性资金推动。

LONG 证据例如：

- Price ↑
- Volume ↑
- Taker Delta ↑
- CVD slope ↑
- OI ↑
- Price Efficiency 正常
- breakout acceptance

SHORT 镜像。

特别区分：

### New Long Build

Price ↑ / OI ↑ / Delta ↑

### Short Squeeze

Price ↑ / OI ↓ / Delta ↑

Short squeeze 不能和真正新增多头资金混为一谈。允许继续上涨，但 signal subtype 必须不同。

同理区分：

### New Short Build

Price ↓ / OI ↑ / Delta ↓

### Long Liquidation

Price ↓ / OI ↓ / Delta ↓

---

# 十六、False Start Filter 是整个系统核心

不要为了尽快触发而弱化这一层。

至少实现以下 Veto：

### DATA_STALE

关键数据不可信。

### RAPID_RETRACE

启动后快速回吐。

### OI_CONTRACTION

声称“新增资金启动”但 OI 明显下降；Squeeze 类型除外。

### DELTA_REVERSAL

主动资金方向快速反转。

### NO_ACCEPTANCE

突破后不能站稳。

### LOW_EFFICIENCY_ABSORPTION

出现巨大主动流量，但价格基本推不动。

### EXTREME_CROWDING

funding / premium 极端拥挤。

### ONE_BAR_SPIKE

只有单根 spike，没有延续。

Veto 输出必须说明：

- 哪条规则
- 使用了什么值
- 阈值
- 时间窗口
- 为什么否决

不能只输出 `Rejected`。

---

# 十七、Continuation Detector

START_CONFIRMED 后继续跟踪：资金是不是仍在进入？

观察：

- OI 是否持续
- CVD 是否持续
- delta 是否维持
- price efficiency
- volume decay
- retrace quality
- higher high / lower low acceptance

状态：`START_CONFIRMED → CONTINUATION`

不要一次确认之后永久保持。

---

# 十八、Exhaustion / Withdrawal Detector

这是和 Startup 同等重要的核心。

目标：主力/方向资金什么时候开始失去推动力？

重点 Evidence：

### CVD Divergence

价格创新高，但 CVD 不创新高。

### OI Decay

OI 从持续增加转为下降。

### Delta Reversal

主动买 → 主动卖。

### Efficiency Collapse

主动资金越来越大，但价格越来越推不动。

### High Volume No Progress

巨大成交量，价格基本不前进。

### Failed Breakout

突破后迅速失守。

### Repeated Rejection

连续攻击同一位置失败。

状态：`CONTINUATION → EXHAUSTION → WITHDRAWAL`

不要简单写 `做多条件消失 = 撤离`，必须有真正的退出 Evidence。

---

# 十九、P2：历史 Replay 必须成为一等公民

同一 Detector 必须可以实时输入和 Replay 输入使用完全相同的核心逻辑。

禁止 `live_detector.py` 和 `backtest_detector.py` 写两套规则。

Replay 输出至少记录：

- symbol
- anomaly_time
- suspected_time
- confirmed_time
- rejected_time
- continuation_time
- exhaustion_time
- withdrawal_time
- direction
- evidence
- veto
- entry_reference_price
- future_max_move
- future_max_adverse_move
- 1m outcome
- 5m outcome
- 15m outcome
- 1h outcome

为以后校准假启动率、启动成功率、续航概率、撤离准确率准备数据。

---

# 二十、现在不要做“机会分”

Dashboard 不显示拍脑袋的 `机会分 87`。

第一阶段显示：

- 状态：`SUSPECTED_START`
- 方向：`LONG`
- Evidence：`6 / 8`
- Veto：`0`
- Data Confidence：`CONFIDENT`

例如：

ONGUSDT

状态：启动确认  
方向：LONG

资金证据：

- Volume Z +4.7 ✓
- Trade Count Z +3.8 ✓
- OI 1m +1.9% ✓
- OI 5m +4.7% ✓
- Taker Delta +$1.8M ✓
- CVD slope +3.1σ ✓
- Price Efficiency 0.76 ✓

Veto：

- Rapid Retrace：否
- OI Contraction：否
- Delta Reversal：否
- Data Stale：否

这种解释比一个 87 分重要。

---

# 二十一、重新设计 Dashboard

暂时不要追求炫酷动画。

首页目标是：10 秒内知道市场里“钱在哪里动”。

顶部：

- DATA LIVE / DEGRADED
- Universe 数量
- 当前异常数
- 疑似启动
- 确认启动
- 延续
- 衰竭
- 撤离
- 数据源健康

主体不要只是一张普通表格，使用 Radar Cards。

每张卡至少：

- symbol
- 24h / 当前变化
- State
- Direction
- 资金进入：强/中/弱
- OI
- CVD
- Delta
- Price Efficiency
- Data Health
- Evidence 数
- Veto 数
- “查看为什么”入口

---

# 二十二、Symbol Detail 页面

点击 symbol 后显示：

## 市场状态

- State
- Direction
- Confidence
- 状态持续时间

## Price

- current
- return
- acceleration
- retrace
- acceptance

## Volume

- RVOL
- Z-score
- acceleration
- trade count

## Flow

- taker buy
- taker sell
- delta
- CVD
- CVD slope

## OI

- current contracts
- Δ30s
- Δ1m
- Δ5m
- velocity

## Context

- funding
- multi timeframe

## Evidence

完整证据。

## Veto

完整否决理由。

## State Timeline

例如：

- 22:31:04 SLEEPING
- 22:35:11 ANOMALY
- 22:35:19 SUSPECTED_START
- 22:35:46 START_CONFIRMED
- 22:39:12 CONTINUATION

---

# 二十三、Dashboard 排序

第一版不要用神秘评分。

排序优先级：

1. START_CONFIRMED
2. CONTINUATION
3. SUSPECTED_START
4. ANOMALY
5. EXHAUSTION
6. WITHDRAWAL
7. SLEEPING

同状态内部排序：

- Evidence count
- anomaly strength
- recency

必须显示排序依据。

---

# 二十四、数据健康页面

新增 `/health`。

至少展示：

| Symbol | aggTrade | Kline | OI | Funding | Queue Lag | Confidence |
|---|---|---|---|---|---|---|

状态：

- LIVE
- DEGRADED
- STALE
- ERROR

每个数据源显示 `last_event_age`。

如果 stale，明确红色。

---

# 二十五、429 / 418 防护

所有 REST 公共接口统一经过 RateLimiter。

必须支持：

- request scheduling
- concurrency limit
- exponential backoff
- jitter
- Retry-After
- 429 handling
- 418 circuit breaker
- cooldown
- metrics

不得每个 Collector 自己随便 retry。

REST 请求失败时不能用假的默认数据，返回 unavailable/stale。

---

# 二十六、WebSocket 生命周期

至少实现：

- auto reconnect
- exponential backoff
- resubscribe
- last message tracking
- last exchange event tracking
- duplicate detection
- stale detection
- connection metrics

不要只判断 `socket.open == True`，必须检测 `now - last_valid_event`。

---

# 二十七、测试要求

修改完成后必须运行全部现有测试，并新增至少以下测试：

## Data

- aggTrade long direction
- aggTrade short direction
- trade dedup
- reconnect
- OI contracts semantics
- stale OI
- stale trades
- queue lag

## Feature

- CVD
- delta
- RVOL
- OI velocity
- price efficiency
- retrace

## Detector

- true long startup
- true short startup
- short squeeze
- long liquidation
- fake pump
- rapid retrace
- OI contraction veto
- delta reversal veto
- absorption veto
- continuation
- exhaustion
- withdrawal

## Integration

Fixture：`raw events → features → detectors → state machine → dashboard`

必须验证 Evidence 不会在 Dashboard refresh 后消失。

## Replay

同样输入重复运行两次，输出必须 deterministic。

---

# 二十八、增加真实运行 Smoke Test

增加可手动运行的 `scripts/live_smoke_test.py`。

默认只测试：

- BTCUSDT
- ETHUSDT
- SOLUSDT

运行 10 分钟。

输出：

- trade count
- duplicate count
- last event age
- kline age
- OI age
- funding age
- reconnect count
- queue lag p50/p95
- Feature sample
- state transitions

结束以后明确打印 PASS / FAIL。

注意：这是 Smoke Test 的 3 个币，生产 Universe 不再是硬编码 3 个币。

---

# 二十九、性能

不要为了“全市场”直接创建几百个无限 Task。

必须观察：

- event/sec
- queue size
- queue lag
- CPU
- memory
- symbol count
- REST request count
- reconnect count

采用 bounded queue。

如果消费者落后，Health 必须降级。禁止无限积压。

---

# 三十、配置化

以下不得散落在代码中：

- universe size
- liquidity floor
- window length
- stale threshold
- anomaly threshold
- OI threshold
- delta threshold
- retrace threshold
- persistence duration
- cooldown
- rate limit
- candidate count

统一进入 `configs/` 或现有配置系统。

---

# 三十一、代码质量原则

不要大规模推倒重写当前架构。

优先：`integrate > replace`

保持：

- collectors
- features
- detectors
- state_machine
- health
- replay
- api

分层。

任何 Detector 都不允许直接请求 Binance。

任何 Dashboard 都不允许直接计算交易逻辑。

任何 Collector 都不允许决定 LONG / SHORT。

---

# 三十二、最终交付文件

本轮结束时至少更新/增加：

- `docs/RUNTIME_INTEGRATION_AUDIT.md`
- `docs/RUNTIME_ARCHITECTURE.md`
- `docs/LIVE_DATA_FLOW.md`
- `docs/FEATURE_CATALOG.md`
- `docs/DETECTOR_LOGIC.md`
- `docs/OPERATIONS.md`
- `docs/KNOWN_LIMITATIONS.md`
- `CHANGELOG.md`

同时更新 README 的项目状态。

不要再写“纯文档阶段”。

README 必须真实反映：

- 哪些数据已经 live
- 哪些仍计划中
- 当前 universe
- 如何启动
- 如何测试
- 当前 limitations

---

# 三十三、最终必须给我一份 Implementation Report

完成代码修改后，不要只说“已完成”。

必须输出：

## 1. Audit Summary

发现多少：

- orphan modules
- demo overrides
- missing integrations
- stale-data risks
- dashboard bugs

## 2. Changed Files

每个文件改了什么。

## 3. Runtime Data Flow

用 ASCII 图展示：

`Binance → Collectors → Health → WindowManager → FeatureEngine → Detectors → StateMachine → Repository → Dashboard / Alerts`

## 4. Real Data Verification

明确说明：

- AggTrade：LIVE / NOT LIVE
- Kline：LIVE / NOT LIVE
- OI：LIVE / NOT LIVE
- Funding：LIVE / NOT LIVE

不得模糊描述。

## 5. Test Results

给出 `pytest` 结果。

## 6. Smoke Test

给出运行结果。

## 7. Remaining Risks

列出仍未解决的问题。

## 8. Next Recommended Work

仅提出下一阶段，不要自动加入一堆新功能。

---

# 三十四、本轮完成定义 DoD

只有同时满足以下条件，本轮任务才算完成：

- 不再硬编码 production 只跑 BTC/ETH/SOL
- SymbolRegistry 真正进入 runtime
- AggTrade live
- Kline live
- OI live
- Funding live
- Data Health 真正工作
- 不存在强制 CONFIDENT
- Evidence 不再被 Dashboard refresh 擦除
- 多时间窗口工作
- FeatureSnapshot 获得真实 OI / Funding / Flow
- Anomaly Detector 使用真实数据
- Startup Detector 使用真实数据
- FalseStartFilter 使用真实数据
- Continuation 使用真实数据
- Withdrawal 使用真实数据
- 数据 stale 时 Fail Closed
- Dashboard 能显示 Evidence 与 Veto
- Replay 保持 deterministic
- 测试全部通过
- Live smoke test 通过
- README 与 Runtime 一致

只要其中任何一项未完成，不要声明 V1 Runtime 已完成。

---

# 三十五、暂时不要做的下一阶段功能

本轮禁止提前实现：

- SMC
- Order Block
- FVG
- Fibonacci
- AI 自动评分
- 机器学习
- Q-learning
- 神经网络
- 自动参数优化
- 自动交易
- 用户账户系统
- 多交易所
- 花哨图表

先把 **Binance 数据真实性 + 量价资金流 + 启动/假启动/撤离** 做扎实。

---

# 三十六、执行顺序

严格按照：

Audit  
→ P0 Runtime Truth  
→ Data Health  
→ OI/Funding/Kline Integration  
→ Multi Window  
→ Feature Validation  
→ Detector Integration  
→ False Start Validation  
→ Withdrawal Validation  
→ Dynamic Universe  
→ Two-stage Radar  
→ Dashboard  
→ Replay  
→ Smoke Test  
→ Documentation

执行。

不要因为 UI 比较显眼而提前做 UI。

---

# 最终目标

本轮完成以后，我打开 Dashboard，不应该只是看到：

- BTC
- ETH
- SOL
- 冷却
- 证据 0

而应该看到类似：

- 市场 Universe：100
- 异常：12
- 疑似启动：5
- 确认启动：2
- 延续：3
- 衰竭：1

并且任意点击一个 symbol，我能够明确回答：

> 为什么认为资金在进入？

> 是新增多头，还是 short squeeze？

> OI 到底怎么变化？

> 主动买卖方向是什么？

> CVD 有没有确认？

> 有没有快速回吐？

> 哪些 False Start Veto 已通过？

> 数据是不是新鲜？

> 资金什么时候开始衰竭？

> 为什么判断撤离？

如果这些问题系统不能用真实数据回答，本轮仍然没有完成。

**开始工作。**
